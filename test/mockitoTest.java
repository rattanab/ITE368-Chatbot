import static org.mockito.Mockito.*;

import org.junit.Test;

public class mockitoTest {
	
	final static chatbotFn testfn = mock(chatbotFn.class);
	final static Chatterbot2 testbot = mock(Chatterbot2.class);
	
	@Test
	public void test() {
//		when(testfn.whatTime()).thenReturn("08:00:00");
//		verify(testbot).main(null);
//		when(test.timer(10, "test").thenReturn(0);
//		verify(test)
	}
}
